'use client';

export default function ProfileBookingsPage() {
  return (
    <div
      style={{
        background: '#fff',
        border: '1px solid var(--color-border-default)',
        borderRadius: 14,
        padding: 18,
        minHeight: 240,
      }}
    >
      Lịch sử đặt lịch
    </div>
  );
}

